// Package cli 是操作员命令行入口，负责参数校验、带凭证的 RPC 和 JSON 输出。
//
// 标准输出用于可机器解析的业务结果，诊断写到标准错误。不要把调试文本混入
// JSON；不要打印机器令牌。RPC 超时或断线表示结果未知，写请求重试应沿用原
// 幂等键或报告 event_id，不能因客户端报错就认定服务端没有提交。
//
// CLI 展示的是服务返回的事实；搜索结果是异步投影，任务列表/详情才是事实
// 查询。退出码用于脚本中断失败步骤，不能仅凭终端出现过 PASS 字样判定成功。
package cli
