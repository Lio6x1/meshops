# Z05-01｜Kafka客户端与有界超时

本页是小步骤的精确文件清单，配合[逐步操作说明](README.md)使用。所有路径相对于同一个学习目录；按表创建或整份替换，已有未列出的文件保留。完整文件直接链接到本步骤使用的真实版本，不需要拼接解释片段。

## 先移除（先停止旧服务，备份放到工程外）

无。

## 创建或完整替换

| 学习目录内路径 | 本步骤完整文件 |
| --- | --- |
| `internal/bus/kafka.go` | [复制完整文件](../../state-stages/z05/internal/bus/kafka.go) |
| `internal/bus/lag.go` | [复制完整文件](../../state-stages/z05/internal/bus/lag.go) |
| `internal/bus/retention.go` | [复制完整文件](../../state-stages/z05/internal/bus/retention.go) |
| `internal/bus/kafka_test.go` | [复制完整文件](../../state-stages/z05/internal/bus/kafka_test.go) |

## 执行

```powershell
Set-Location 'D:\job\golang\projects\meshops-course-lab'
$env:Path = 'D:\go1.25.10\bin;' + $env:Path
$env:GOWORK = 'off'
go build ./...
if ($LASTEXITCODE -ne 0) { throw 'build failed' }
go test ./internal/bus -run '^(TestOperationsHonorContextWithSilentBroker)$' -count=1 -v
if ($LASTEXITCODE -ne 0) { throw 'test failed' }
```

构建成功通常没有输出。测试必须出现下列顶层测试的PASS；[no tests to run]不是通过本步骤。
- `TestOperationsHonorContextWithSilentBroker`
