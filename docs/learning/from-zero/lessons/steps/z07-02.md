# Z07-02｜任务事务、查询与Outbox

本页是小步骤的精确文件清单，配合[逐步操作说明](README.md)使用。所有路径相对于同一个学习目录；按表创建或整份替换，已有未列出的文件保留。完整文件直接链接到本步骤使用的真实版本，不需要拼接解释片段。

## 先移除（先停止旧服务，备份放到工程外）

无。

## 创建或完整替换

| 学习目录内路径 | 本步骤完整文件 |
| --- | --- |
| `internal/tasks/service.go` | [复制完整文件](../../business-stages/z07/internal/tasks/service.go) |
| `internal/tasks/store.go` | [复制完整文件](../../business-stages/z07/internal/tasks/store.go) |
| `internal/tasks/queries.go` | [复制完整文件](../../business-stages/z07/internal/tasks/queries.go) |
| `internal/tasks/migrations.go` | [复制完整文件](../../business-stages/z07/internal/tasks/migrations.go) |
| `internal/tasks/workers.go` | [复制完整文件](../../business-stages/z07/internal/tasks/workers.go) |
| `internal/tasks/domain_test.go` | [复制完整文件](../../business-stages/z07/internal/tasks/domain_test.go) |
| `migrations/001_initial_schema.sql` | [复制完整文件](../../business-stages/z07/migrations/001_initial_schema.sql) |
| `migrations/002_framework_contracts.sql` | [复制完整文件](../../business-stages/z07/migrations/002_framework_contracts.sql) |
| `migrations/003_implementation_contracts.sql` | [复制完整文件](../../business-stages/z07/migrations/003_implementation_contracts.sql) |
| `migrations/004_history_retention_indexes.sql` | [复制完整文件](../../business-stages/z07/migrations/004_history_retention_indexes.sql) |

## 执行

```powershell
Set-Location 'D:\job\golang\projects\meshops-course-lab'
$env:Path = 'D:\go1.25.10\bin;' + $env:Path
$env:GOWORK = 'off'
go build ./...
if ($LASTEXITCODE -ne 0) { throw 'build failed' }
go test ./internal/tasks -run '^(TestInspectValidation|TestTaskTransitionAuthorityAndTerminalBarrier|TestTaskCursorBoundToTenantFilterAndPosition|TestHistoricalMigrationsRemainExecutable)$' -count=1 -v
if ($LASTEXITCODE -ne 0) { throw 'test failed' }
```

构建成功通常没有输出。测试必须出现下列顶层测试的PASS；[no tests to run]不是通过本步骤。
- `TestInspectValidation`
- `TestTaskTransitionAuthorityAndTerminalBarrier`
- `TestTaskCursorBoundToTenantFilterAndPosition`
- `TestHistoricalMigrationsRemainExecutable`
