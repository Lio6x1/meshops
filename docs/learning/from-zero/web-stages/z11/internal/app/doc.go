// Package app 将配置与依赖装配成可运行进程，不承载任务或实体业务规则。
//
// Run 先验证配置、凭证和依赖，再构造业务服务并注册 gRPC。worker、健康检查
// 与 RPC 服务器共享取消生命周期；worker 意外退出必须使进程失败，而不能
// 留下仍能响应健康探针、却不再消费数据的服务。依赖临时故障由相应模块重试，
// readiness 同时反映依赖可用性。退出时先撤销就绪、取消后台工作，再限时排空
// RPC，最后按创建的逆序关闭数据库、Redis、Kafka 和客户端连接。
//
// Search 只有在引导标记完整、索引 UUID 匹配后注册；Entity 的重建模式在维护
// 窗口执行，完成后退出，不与普通投影同时运行。
package app
