# 剩余阶段课内步骤验证

本轮细化Z05、Z06、Z08、Z09，共新增11步；与之前Z04/Z07九步合为 [20个步骤](../lessons/steps/README.md)。完整文件链接、命令、依赖接线和输入推演见 [新增步骤讲解](../lessons/steps/remaining.md)。最终业务源码未变，没有新增实体、任务类型或中间件。

## 实际结果

`verify-substeps.ps1`在一个新目录从Z01推进到Z09，Z04以后全部按小步骤复制。每步编译整个当前工程、执行精确指定的顶层测试并解析Go JSON确认PASS；每阶段结束核对文件总数与全部SHA256，包含二进制基线。

| 阶段 | 构建步骤 | 指定顶层测试执行次数 | 本轮依赖范围 |
| --- | ---: | ---: | --- |
| Z04 | 4 | 7 | 本地规则与bufconn RPC；协议步无业务测试 |
| Z05 | 2 | 3 | 静默TCP端点超时；真实Redis/Kafka |
| Z06 | 4 | 14 | 六类转换、bbolt、gRPC测试流、真实Kafka/Redis |
| Z07 | 5 | 15 | 规则、游标、SQL解析、分发队列、inbox与测试RPC |
| Z08 | 2 | 3 | 采样规则、真实MySQL历史去重/分页与保留清理 |
| Z09 | 3 | 7 | Redis影子预期校验；验收工具失败传播与预算 |
| 合计 | 20 | 49 | 全部通过，Go JSON中SKIP事件为0 |

49是执行次数，包含跨步骤重复验证，不等于49个不同函数。阶段末所有源文件均与已验证阶段一致，Z07-01的临时三测试版本在下一步替换回完整文件。

成功报告与原始逐步日志：[result.json](../../../../.cache/substeps-9634d1c2891149e0b7087e61bc41104d/result.json)。教师运行使用Redis16379、Kafka19092；学生Z05/Z06用自己的阶段compose26379/29092，讲义明确区分。MySQL通过ADMIN_DSN触发已有TestMain创建隔离数据库并迁移，测试结束清理生成库，未在业务库执行历史清理。

结束检查：20份步骤页与来源映射一致，全部来源hash未变；1063个本地文档链接有效，两个修改的维护脚本语法通过，15个正文代码块及202个阶段完整代码块与源文件一致。MySQL、Redis、Kafka均healthy，课程业务进程记录为空。

## 首次环境失败与恢复

首次运行到Z05-02失败：Redis16379拒绝连接，Kafka不可用。检查docker compose ps显示课程MySQL、Redis、Kafka均已退出（255）。这不是业务逻辑测试通过，失败记录保留在 [首次result.json](../../../../.cache/substeps-5aca6d025a124ea6a72c3158fb20c974/result.json)。

执行参考环境 `docker compose up -d --wait --wait-timeout 180` 后三项依赖健康，再从新的空目录完整顺序验证，得到上面的成功记录。没有通过改测试、跳过真依赖或删除数据解决；本轮未停止依赖，未启动常驻课程业务进程。

## 不能据此推出的结论

- Z06-04指定的ACK/投影测试不直接验证订阅，完整六类网关和watch操作仍按讲义及已有阶段运行证据复核；不能将其算作全部订阅故障验收。
- Z09-01使用独立generation验证manifest与墓碑，不切换业务active指针；完整影子重放/切换仍继承原隔离验收记录。
- Z09工具测试验证恢复控制流、错误传播、预算和报告失败，不是重新进行完整容器故障或性能实验。
- 本轮没有改变最终业务源码，因此不重复已有的全量故障/压测。完整工程证据见 [参考验收](../../../../verification/2026-09-10/summary.md)。

下一步是全课程操作路线与讲解终审，尤其多终端、配置和启动的衔接；不能将20次构建简单换算成“学生已理解全部代码”。
