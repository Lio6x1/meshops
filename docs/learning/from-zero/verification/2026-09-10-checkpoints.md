# 递进检查点验证 · 2026-09-10

Z01—Z09 已在同一个从空目录建立的学习工程中顺序应用，并完成各阶段对应运行验证。最终复制的143个源码、配置和测试文件与此前验收基线一致；旧指纹表另外包含1份历史测试输出 `internal/tasks/integration-results.txt`，它按复制规则排除，不属于运行代码。

本轮默认单人执行，没有启动子 agent。关键业务的故障、性能和 A01—A28 证据复用 [已完成的工程验收](../../../../verification/2026-09-10/summary.md)，因为最终业务源码没有变化；本轮重点验证课程切片、文件升级与运行衔接。

## 实际证据

主记录：[result.json](chain-9d09cc23db174b59835bca6f49bf9111/result.json)，passed=true，九阶段完成。文件数量与最终源码比对：[source-check.json](chain-9d09cc23db174b59835bca6f49bf9111/source-check.json)。阶段目录和操作步骤见 [CHECKPOINTS.md](../CHECKPOINTS.md)。

| 阶段 | 检查 | 实际结果 |
| --- | --- | --- |
| Z01 | 生成包构建 | 通过；本阶段没有业务测试 |
| Z02 | 同一学习目录真实进程查询 | 固定人员与不存在 ID 正确，见 [smoke](chain-9d09cc23db174b59835bca6f49bf9111/z02-smoke.txt) |
| Z03 | 真实写入/查询/更新/错误/重启 | 人员无电量、无人机零电量、非法纬度拒绝、重启丢失符合阶段边界，见 [smoke](chain-9d09cc23db174b59835bca6f49bf9111/z03-smoke.txt) |
| Z04 | 鉴权上报到内存查询 | 新版本可查，人员无电量，错误凭证拒绝，见 [快照](chain-9d09cc23db174b59835bca6f49bf9111/z04-snapshot.json)与[拒绝输出](chain-9d09cc23db174b59835bca6f49bf9111/z04-unauthorized.txt) |
| Z05 | 独立 Ingest/Entity，真实 Kafka/Redis | 等待 Kafka ACK 后真实投影可查，错误凭证拒绝，见 [快照](chain-9d09cc23db174b59835bca6f49bf9111/z05-snapshot.json) |
| Z06 | 六类来源分别离线产生，再同 DB 补传；真实订阅 | 六类均可查，补传成功；[人员补传](chain-9d09cc23db174b59835bca6f49bf9111/z06-person-drain.txt)、[订阅](chain-9d09cc23db174b59835bca6f49bf9111/z06-watch.out.txt)含 SNAPSHOT_END，其余五类记录在同目录 |
| Z07 | 新建独立 Compose 数据卷，迁移/seed/启动/演示 | 六类查询、四类任务 effect_count=1；状态历史命令拒绝，见 [演示](chain-9d09cc23db174b59835bca6f49bf9111/z07-demo.json) |
| Z08 | 原学习目录升级、保留凭证/队列/数据 | 重复初始化通过，任务继续完成，状态历史真实返回样本，见 [演示](chain-9d09cc23db174b59835bca6f49bf9111/z08-demo.json)与[历史](chain-9d09cc23db174b59835bca6f49bf9111/z08-history.json) |
| Z09 | 同一学习目录最终升级 | 八个程序构建、完整演示与历史查询通过，见 [演示](chain-9d09cc23db174b59835bca6f49bf9111/z09-demo.json)与[历史](chain-9d09cc23db174b59835bca6f49bf9111/z09-history.json) |

Z01—Z08 的 `go test ./... -count=1` 均已执行并退出0；Z04—Z06 同步共享代码后再运行相关阶段测试也退出0。这里单元测试未设置真实依赖变量时会跳过相应集成用例，不能将这些跳过记为通过；表中真实运行证据来自进程/容器链路验证。

## 复制工具修复

`test-checkpoints.ps1 -AllStages` 通过，覆盖解析、顺序升级、重复应用、拒绝跳级、学生修改保留、未受管理文件冲突、笔记保留、旧文件备份、状态记录路径越界和父目录 junction 拒绝。

已实际复现再修复：原 `$Stage:` 插值导致脚本无法解析；不存在的目标目录通过祖先 junction 重定向时未被拒绝。Z07/Z08 现在分别指向实际阶段目录，缺目录直接报错，不再退回完整 reference。

## 本轮中断与续验说明

第一次连续执行已通过 Z01—Z08。Z08 的手工历史查询载入 `scripts/env.ps1` 时，验收驱动自己的 `$courseRoot` 被同名变量覆盖，因此进入 Z09 时找不到复制脚本，恢复依赖的配置路径也出错。业务演示结果没有伪装成整个流程通过，首次结果为 passed=false。

随后显式恢复参考容器，把驱动目录变量改为独立的 `$chainMaterialRoot`，修复清理失败传播，并从同一个学习目录续验 Z09。没有清空前八阶段数据，也没有重跑并覆盖它们的有效证据。最终记录汇总前八阶段及续验的 Z09；[恢复日志](chain-9d09cc23db174b59835bca6f49bf9111/reference-restore.txt)确认参考依赖均健康，独立测试容器已停止、卷保留。

## 不能从本轮结果推出什么

- 这证明九个阶段答案能够在同一目录递进运行；逐课讲义、每课的小切片和学生理解尚未验收。尤其 Z07 一次引入多个任务组件，需要继续拆成多课讲解。
- 早期的内存及隔离状态环境不承诺数据自动迁移到 Z07；该切换在阶段说明中明确要求重新 seed/生成模拟状态。Z07→Z08→Z09 才沿用同一运行环境和持久文件。
- 未对中间阶段重新运行最终工程的全部故障和压测；最终源码指纹一致，工程验收继承此前结果，课程新增运行路径按本页验证。
- 未远程执行 CI，未由外部学生完成所有课稿的逐字复制。不能把已测试代码目录等同于已完成课程。

下一步先补齐 Z01—Z03 的正文代码占位标记及路径说明，再按变化清单编写 Z04—Z09 的逐课完整文件与编号解释。最后从空目录按课稿而非直接套阶段答案进行全程复制并终审。用户最新指示为后续统一使用 Astra Medium，覆盖此前的模型切换安排。
