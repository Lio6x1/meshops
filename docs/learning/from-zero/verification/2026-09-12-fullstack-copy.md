# 前后端课程复制验收

本次通过完整文件讲义在新的临时学习目录依次建立 Z01—Z13，并完成 Go 构建、配置的指定测试及 Z12/Z13 前端安装、测试和生产构建；另从冻结 Z10 验证新增五个细分步骤。逐项结果和原始来源 SHA256 见[结构化证据](2026-09-12-fullstack-copy.json)。这验证参考代码的可复制性，不代替学习者理解或真实运行验收。

最终清单为 Z11 242 文件、Z12 270 文件、Z13 299 文件；13 阶段合计 1676 条指纹记录、350 个完整代码块、28 个细分步骤。历史全复制结果中的 273/302 含后来排除的三个 npm 缓存文件，不能继续用作最终源码数量。

完整复制通过之后，最后变化为 `recovery.go` 三处错误文字首字母，以及 Git/Docker 对 `.npm-cache` 的排除。已从最新讲义提取对应完整代码块，更新两个已通过的临时学习目录，逐一核对最终 299 个文件与发布指纹；Markdown 路线允许 CRLF/LF 与末尾换行归一化，细分步骤路线逐字节核对。两个目录再次运行 `go test ./cmd/demo-init -count=1` 均通过，保留原始整套运行结果和增量证据，没有改写旧结果。

过程中修复了两处教学工具问题：应用阶段记录仍只允许 Z01—Z10，导致 Z11→Z12 被拒绝；发布器未排除嵌套 npm 缓存，导致缓存混入源码清单。前者通过全阶段应用/重复应用/编辑保护测试复验，后者从发布范围排除并移除误生成的教学缓存副本，根前端缓存未删除。一次 npm 安装还因全局缓存无写权限失败，改用本次进程的项目内缓存后完整运行通过，未更改系统配置。

原始 JSON 已按哈希复制到工程私有 `.local/archives/fullstack-20260912/course-copy/`，本页旁的脱敏证据纳入 Git。真实页面、四类任务、搜索同步、重启与故障恢复见[全栈验收](../../../verification/2026-09-12-fullstack/report.md)。
