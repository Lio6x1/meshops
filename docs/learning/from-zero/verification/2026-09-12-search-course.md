# Z10 教材与复制验收

## 发布范围

Z09 固定快照 168 个文件；Z10 使用根工程的明确源码清单，共 206 个文件，增加 38 个、替换 11 个，没有移除 Z09 的业务文件。发布的 CI 已接入 ES；新增内容不倒灌前九个阶段。

全课程十个阶段的完整答案共有 250 个文本代码块。生成的 Go 文件、二进制协议基线和历史验收记录使用明确原文件链接。临时 `internal/tasks/integration-results.txt` 曾被新发布器误纳入，受管理复制检查拒绝后已排除，并增加防泄漏检查；不把它当课程源码。

Z10 主讲义按协议、CDC、版本/提交、PIT/权限、快照、接线、真实运行和排错展开；三个可构建检查点对应协议、搜索包、整体接线。加上原 20 个步骤，当前共 23 步。

## 已通过的复制检查

- `.cache/course-copy-97e07a99a65b4d2c93cfd55d62a3b16e/learner`：受管理路线 Z01→Z10，Z10 重复应用幂等，学生改动保护、备份、跳级拒绝、路径/junction 防护，未复制凭证、Git 元数据或临时测试输出。
- `.cache/lesson-copy-d0cf8876fb3d44b3a1bbda1eaa8d066d/result.json`：从十阶段 Markdown 完整代码块和指定文件链接还原，同一目录逐阶段构建通过；Z10 最终 206 个文件。
- `.cache/substeps-0e6bda9aa43e4abcb7166a6d982596de/result.json`：从固定 Z09 起逐个复制 Z10-01、02、03，三次构建、九个指定测试执行通过。协议步不包含业务测试，不计为测试场景。
- `.cache/lesson-copy-71ca836cdbb548c5b62998cf971f552d/result.json`：演示脚本增加新鲜状态等待后的最终文本还原检查；仅做文件核对，未重复全部构建。
- `.cache/search-rebuild-20260912/proto-final.txt`：Buf lint、相对两个既有基线的兼容性检查，以及 protoc 逐字节重新生成通过。

普通业务单元测试、真实搜索集成测试和维护恢复见 [维护验证](2026-09-12-search-rebuild.md)。不能把这些独立口径相加后宣传为独立业务场景数量。

## 独立环境运行

维护脚本 `verify-search-course.ps1` 为全新学习目录分配独立 Compose 项目和数据卷，先初始化 Z09、完成六类实体和四类任务演示，再原目录升级到 Z10。脚本检查凭证未变、升级前任务已导入、升级后任务增量同步，并执行索引丢失重建。最后停止测试环境、恢复先前运行的参考依赖，不删除数据卷。

首次运行 `.cache/search-course-becee877564946828271653d627fe0b6/result.json`：升级、旧任务导入、新任务同步和重建校验通过；重建后立即创建任务被 Task 以实体状态过期拒绝，整次标记为失败，清理错误为零。随后修正 `demo-search.ps1`：最多等待 45 秒，确认无人机的快照仍有至少 5 秒有效期且支持 inspect，再创建任务。保留 Task 原有新鲜度检查，不增加硬编码延时来掩盖问题。

修正后的 `.cache/search-course-c904ec1ac4374edd8af3d3bca449df8f/result.json` 为 `passed=true`、`cleanupErrors=[]`。四项检查全部完成：全新 Z09 业务演示；原目录升级且凭证不变、旧任务导入、新任务同步；未完成引导与索引丢失恢复、业务表校验和不变、重建后增量继续；学习目录发布源码指纹不变。测试项目已停止，原来运行的参考依赖已恢复。

此后仅更新 README/验收导航与 CI 的 ES 环境接线，不改已验证业务实现。最终文件答案重新发布核对。CI 已配置 ES 集成依赖，但没有在本次本地任务中推送或运行 GitHub Actions；不要将配置完成写成远端 CI 已通过。最终复核范围见 [工作记录](../BUILD-LEDGER.md)。
