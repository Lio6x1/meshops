# 教材结构清理记录 · 2026-09-11

用户要求先整理结构，并明确对当前课程无用的旧材料直接删除。本轮完成教材清理与入口统一，不代表全项目代码审查或全部课程最终复核完成。

## 删除范围

删除 `docs/learning/lessons/` 的旧 L01—L50 讲义、`docs/learning/examples/` 的独立机制示例模块及关联 IDE 文件；删除旧 00 练习、01—05 课程册、旧课程目录、V2 说明和旧教材验证页。本轮一度新建的旧资料入口也已撤销，不再保存第二套旧材料导航。

删除前核对绝对路径均位于本仓库 `docs/learning/` 内，检查文件指纹与目录重解析点后执行。共删除 119 个文件（其中 2 个是本轮刚建立后撤销的导航文件），约 599 KB。清单只保存路径、字节数和 SHA256，不备份一套旧内容。

## 保留与整理

- 统一 [仓库首页](../../../../README.md)、[学习入口](../../README.md) 和 [课程目录](../lessons/README.md)。
- 增加 [材料导航](../README.md) 和 [维护说明](../maintenance/README.md)，明确完整工程、阶段快照、讲义与维护脚本的职责。
- 重写教学路线、验收映射与当前工作记录，修正交接入口、学习记录及补课编号，移除失效引用。
- 保留完整参考工程、九个阶段源码、20 个小步骤、生成工具和业务/课程验收证据。阶段副本用于教学，不是重复部署。
- 根骨架代码、数据库、运行数据与构建缓存没有在本轮删除。根骨架是否退出仓库需要另核对根 CI、脚本和规格依赖，不能以教材清理代替该判断。
- Canal/ES 搜索仍为下一阶段待实现项，未新增服务占位或改业务逻辑。

## 本轮验证

在写入本记录前，对仓库首页与全部 docs Markdown 执行本地链接目标存在性检查：94 份文档、1265 个本地链接，0 个缺失。代码围栏先剔除；该检查不声称验证页面锚点或外部网站。

- `publish-lesson-code.ps1 -Check`：15 个正文完整源码块一致。
- `publish-file-guides.ps1 -Check`：9 阶段共 202 个完整文件代码块一致。
- 根据 `checkpoint-index.json` 检查 9 阶段的 594 个文件条目，全部 SHA256 匹配；包含二进制基线。
- 根据 `substep-index.json` 检查 20 步的 214 个文件条目，全部 SHA256 匹配。条目可能跨阶段重复，不是独立源文件数量。
- 指定文档的 `git diff --check` 退出 0；Git 不包含尚未跟踪文档的差异，未将其作为全部文档验证的唯一依据。

本机清理清单与静态检查结果保存在仓库 `.cache/organization-20260910/`，目录按整理开始日期命名。业务源码未变，本轮没有重新运行完整集成测试、停容器故障演练和性能测试；其既有结果仍以相应报告为准。
