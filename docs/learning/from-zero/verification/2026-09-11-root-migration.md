# 完整工程迁入仓库根目录 · 2026-09-11

用户授权只留下新工程。本轮将完整实现由教材内的 reference 目录迁入仓库根目录，删除旧骨架、旧 Makefile 与旧 CI；保持 module 为 `example.com/meshops-course`。新版阶段答案继续保留，Z09 直接引用根目录发布清单。

## 实际变化

- 迁移前列明旧骨架 103 个文件，核对安全路径、文件指纹和进程状态后删除旧目标；迁移后立即核对 169 个新工程文件 SHA256 全部一致。
- 新工程的 `.local`、`data`、`results` 和构建产物随迁移保留，没有清空 bbolt 数据或 Docker 卷。后续 README 与 ignore、CI、课程元数据按新位置调整。
- 删除 3 份失效的骨架导航/教学计划文件和停用的 Python 阶段生成器，修正引用。保留仍有用途的业务契约、设计依据、阶段答案和验收记录。
- 根 CI 采用原完整参考工程的构建、检查、Linux 竞态及真实依赖验收流程，改为根目录运行。未声称已在远端 Actions 执行。
- 最终阶段复制改为明确文件清单，避免扫描整个仓库复制 Git、教材、凭证、缓存。仓库内部只允许 `.cache` 下的独立测试目标，课程目录、业务目录和路径穿越仍拒绝。
- 用 `substeps/go.mod` 将不完整教学片段隔离在根模块测试范围外；片段仍在对应完整学习工程里验证。
- 最终阶段需要替换根 `.gitignore`，因此完整文件展示从 202 增为 203 个文本代码块。阶段发布文件数量仍为 Z09 的 168 个，不把全仓文档和运行数据作为阶段文件复制。

## 失败复现与修复

第一次整阶段复制在 Z09 因寻找已迁走的目录失败，调整源位置及清单复制后通过。第一次根目录 `go test ./...` 把教材中的部分测试文件当成业务包，出现缺少定义；加入教学片段模块边界后根测试通过。没有删除这些测试来掩盖问题。

根初始化第一次被沙箱 Docker 权限拒绝，按照审批流程在沙箱外重跑并成功。Go 曾输出模块缓存写入权限警告，但最终根构建、测试与 vet 退出 0；没有将警告当作功能验收结果。

## 本轮实际验证

| 检查 | 结果 |
| --- | --- |
| 根目录 `go build ./...` | 通过 |
| 根目录 `go test ./... -count=1 -timeout=5m` | 通过；本轮普通单元/本地 RPC 测试，不作为完整外部依赖测试 |
| 根目录 `go vet ./...` | 通过 |
| `test-checkpoints.ps1 -AllStages` | 通过，包括顺序、重复应用、编辑保护、备份、路径边界，以及最终清单不复制 docs/Git/凭证 |
| `verify-file-guides.ps1 -Build` | 新目录顺序恢复 Z01—Z09，9 次构建及 Z02/Z03 RPC smoke 通过 |
| 正文与完整文件同步 | 15 个正文源码块、203 个文件代码块一致 |
| 源码指纹 | Go/Proto/SQL、模块和 Compose 共 114 个文件与迁移前一致；9 阶段 594 个条目、20 步 215 个条目均匹配现行清单 |
| PowerShell 语法 | 56 个脚本解析无错误 |
| 根初始化、启动、完整演示、停止 | 通过；6 类实体、4 个成功任务，演示结果 `passed=true` |
| 进程清理 | `.local/processes.json` 为空 |
| 文档链接 | 写入本记录前 91 份文档、1227 个本地链接目标均存在；不包含外网或锚点检查 |

复制证据位于仓库 `.cache/lesson-copy-f28a0764bee945ae8182d5a231e025a4/result.json`，受管理复制目录为 `.cache/course-copy-886d2ede0bf744f29087102aea40865a/learner`。迁移清单与命令输出在 `.cache/root-migration-20260911/`，实际业务演示在 `results/demo-7d36712b-39fa-472b-8ee3-54734d3179b3.json`。这些是本机文件，迁移清单只记录路径和指纹，不复制旧源码。

本轮没有重跑完整停容器故障测试或性能压测；业务源码未变，其既有证据继续保留在仓库根目录 `verification/2026-09-10/`。历史报告中的执行路径属于当时现场，当前运行入口以根 README 为准。

## 仍待完成

1. 一个搜索服务、一个任务索引的 MySQL/Canal/Kafka/ES 搜索与同步闭环。
2. 搜索对应的详细、可复制课程。
3. 包含搜索的完整项目与教程统一复核和验收。

迁移验收不表示上述三项已完成，也不更新用户实际学习进度。
