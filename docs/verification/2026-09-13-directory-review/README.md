# 目录与当前文档一致性整理

日期：2026-09-13。检查基线为 `dd7649a`；检查范围是目录用途、当前说明、课程导航、历史状态与本机遗留副本，不是新一轮业务正确性或容量验收。

## 已处理内容

- 学习入口统一为 Z00—Z13、13 个代码阶段、28 个分步操作；Z00 只讨论需求。移除“搜索待实现”“全部讲解仍待最终复核”等不再反映当前交付的笼统状态，改为链接实际验证范围。
- 重写 implementation 导航、运行说明和学习进度说明；保留 C01—C11、O01—O05、A01—A28 的有效规则，删除旧 app 配置、占位命令、F/L 50 课提示及全未开始的 S/T/V 表。
- 更新六类适配器、Search、HTTP/SSE 网关、Vue、当前阶段数量及混合压测范围；旧报告保留原日期结果，不倒填新成绩。
- 删除已被 Vue 成品取代的离线原型三个文件；`docs/ui/` 改为当前前端说明。历史交付报告保留当日事实，并说明原型可从 Git 历史追溯。
- 增加 [文件导航](../../README.md) 和 [设计记录状态](../../superpowers/README.md)。百万实体计划明确暂停、未实施、未执行，不能当作容量成绩或继续开发授权。

## 本地清理与证据保留

先归档源文件、日志、JSON 结果和临时学习数据库，再清理可重建的旧复制工程、过期最小框架及生成脚本、Proto 对比目录和空 worktree 目录。未清理现行工具/Go 缓存、根业务代码、当前 `.local` 凭证、`data` 队列、Docker 数据卷及已有公开验收。

共删除 50 个顶层目标，逻辑文件大小 **4,895,889,307 字节**。归档 **11,420,937 字节**，5908 个归档文件逐一通过 SHA-256 内容核对，ZIP CRC 检查通过；明细见 [cleanup.json](cleanup.json)。逻辑大小不等于操作系统即时报告的可用空间变化。

本机保留 `.local/archives/directory-review-20260913/retained-context.zip`，SHA-256 为 `80517cd7a2da76c8f7ff87119fb6ae9557184d476672f23dff577299e33499f6`。它包含临时学习环境上下文，未作为公开附件提交；本报告和清理明细不包含凭证值。既有业务证据仍留在原公开目录，未因本次清理失去公开来源。

两处旧复制保护测试目录含目录链接，本次排除，不递归删除或访问链接目标。最初归档检查因该保护中止；第二次遇 Windows 长路径读取限制，改用长路径读取后完成校验。两次中止均发生在删除前。最终 PowerShell 删除前再次检查所有路径位于本仓库允许范围且无重解析项，删除后逐项确认不存在。

## 验证范围

- 全仓 Markdown 本地链接与锚点检查结果见 [links.json](links.json)。外部链接未联网核验。
- `verify-git-checkpoints.py`：1714 条阶段指纹、13 个阶段一致；最终业务源码和阶段文件未修改。
- `git diff --check` 检查空白差异；另做文档差异复查，核对阶段数、实体类型、输出格式、当前接口与历史状态。
- 本次没有运行 Go/前端测试、Docker 故障演练或压测，不将文档检查写成业务回归通过。此纯文档及废弃原型删除提交使用 `[skip ci]`，不宣称新的云端 CI 成绩。

历史设计和验收不是无用旧代码，仍按日期保留；使用当前工程时从文件导航进入，不按历史实施计划重新搭骨架。
