# 2026-09-13 目录与文档收尾

检查基线：本地 `ac74a96`，开始时工作树干净，只有主工作树；本次只整理文档和本地临时产物，不改业务逻辑。

## 删除与保留

已删除 7008 个缓存/临时文件，合计 1,062,506,066 字节（约 1013.3 MiB），并移除空 `.worktrees/`。逐项结果见 [清理清单](cleanup.json)。删除前检查绝对路径均位于本仓库，空工作树目录也核对了 `git worktree list`。

- `.cache/go-build-reference/`：可重建的旧 Go 编译缓存；未发现正在运行的 Go 构建进程。
- `.cache/lesson-copy-.../`：自动生成的临时学习副本；删除前将其唯一结果另存为 [历史复制记录](historical-copy-result.json)，原文件 SHA256 与脱敏说明见 [归档说明](archive-note.json)。该结果仅证明当时复制一致，`built=false`，不宣称构建通过。
- 保留阶段答案和按日期的验收报告：它们用于从零课程和追溯结果，不是旧工程残留。
- 保留 `.local/` 的凭证、私有归档、进程/维护记录，以及 `data/`、`results/`：它们可能用于恢复或追溯，不因文件名旧而删除。
- 保留 `.cache/tools/`、`.cache/fullstack/tools/`、npm 缓存、`web/node_modules/`：属于可继续使用的工具和依赖；`.idea/`、`.claude/` 属于本机设置。

删除后 `.cache/` 约 109.3 MiB，`.local/` 约 43.0 MiB，前端依赖约 116.3 MiB。这是相应目录文件大小，不包含 Docker 数据，也不是整个磁盘占用。未进行全局 Docker prune，未修改演示容器或相邻学习工程。

## 修正文档

1. README 的前端入口改为已接真实 HTTP/SSE 后端的 Vue 控制台。
2. 启动、账号、面试速记及课程维护说明移除“账号版本教程验收仍待完成”的过时进度，保留原测试版本与范围。
3. 工程能力表补充 Kafka 批量提交、同机单/三实例对照及当前验证边界；将 1790/356/428 的账号版本历史统计与当前 1793/359/432 区分。
4. 重新发布最终阶段 README、指纹、完整代码页及分步清单；没有将新实现倒灌到旧教学阶段。历史验收记录不重写成今天的结果。

## 验证与未发布事项

- 13 阶段共 1793 条文件指纹通过，432 个完整代码块一致性通过；分步清单仍为 28 步。
- 本地文档链接检查见 [链接检查结果](local-links.json)；外部网站不在此次检查范围。
- `git diff --check` 通过；网页 `http://localhost:18090/` 返回 HTTP 200，只证明入口可访问，不冒充整套功能复验。
- 本次没有修改业务代码，不重复运行性能实验、全栈功能或全课程构建。最近实际代码验收以 [消费优化报告](../../../verification/2026-09-13-consumer-optimization/README.md) 为准。

此前约定的功能与消费优化工作已有实现或明确实验结论，没有继续扩功能的隐含任务。仍需区分以下事项：消费优化提交尚未推送到远端，旧 CI 不验证新提交；正在运行的演示未在此次重新部署，不能假定它已经包含最新优化。2000 events/s 仍不稳定、百万容量尚未通过，是已记录的容量边界，不是通过清理能够消除的问题。

根目录仍没有 LICENSE；选定授权条款后再添加，不能擅自替作者选择许可。本次没有修改或联网核查 GitHub topics。以上发布完善不阻止继续学习与本地运行。
