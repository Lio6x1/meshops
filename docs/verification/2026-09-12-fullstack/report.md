# 全栈本机验收与证据记录

本轮已完成下表对应的本机运行与证据归档，共 16 份已结束的结果来源，另保存集成门禁定义。脱敏逐测试结果、来源原始字节 SHA-256 和报告退出码见 [结构化结果](results.json)，生成方法及本地原文位置见 [归档说明](README.md)。这些记录覆盖完整演示工程的具体运行，不是“所有功能、所有环境均无缺陷”的声明。

## 已实际完成的检查

| 检查 | 实际结果 | 适用范围 |
| --- | --- | --- |
| Linux 普通 race | 命令退出 0；顶层 122 PASS、21 SKIP、0 FAIL | 普通测试含依赖门控跳过，不能替代真实依赖集成竞态。 |
| Windows 隔离真实依赖集成 | 命令退出 0；顶层 **168 PASS、3 helper SKIP、0 FAIL**；含子测试 259 PASS | 19 项必需测试全部执行，无业务测试跳过；包与已启动测试都有结束事件。 |
| 首次 HTTP 全流程 | 10/10 检查通过，退出 0 | 操作员/管理员会话与权限、六类实时实体、四类 inspect 成功、执行中取消、任务历史/分发信息、CDC 搜索收敛、会话撤销。 |
| 搜索重建后 HTTP | 独立运行 10/10 检查通过，退出 0 | 新一轮真实业务及搜索闭环，并非复用重建前响应。 |
| 整栈重启后 HTTP | 独立运行 10/10 检查通过，退出 0 | 重启后重新登录并完成业务流程。 |
| 搜索维护重建保留检查 | 9/9 具名检查通过 | 四项已完成任务及其搜索结果保留，另一项取消任务保留。 |
| 整栈重启保留检查 | 9/9 具名检查通过 | 上述已有任务与搜索结果在停止/再次启动后保留。 |
| 真实浏览器操作 | 13/13 具名检查通过 | 登录、六类实体与 SSE 初始完成、浏览器创建无人机任务、成功历史及一次副作用、关键词搜索、旧会话失效、管理员状态、搜索中断及恢复、两种视口宽度。 |
| 运行 UID 文件边界 | 6/6 具名检查通过 | UID 10001、10002、999 对各自运行/浏览器/MySQL 凭据文件的读取及拒绝边界，重启后浏览器访问码保留。 |
| 最终 release 镜像构建 | 命令退出 0 | 包含最后三处错误文字首字母小写修正。 |
| 最终 release 实际启动 | Up-NoBuild 命令退出 0 | 使用已构建的最终镜像启动，完成健康与宿主 HTTP 检查。 |
| 最终 release 保留检查 | 9/9 具名检查通过，退出 0 | 在这些最终镜像上检查已有任务状态、执行结果与搜索保留。 |

Windows 顶层统计排除子测试和包级 `[no test files]`，259 是包含子测试的 PASS 条目数量，不是另一套独立用例数。三个 helper SKIP 为 edge/state/tasks 中的具名强杀测试子进程入口；其父测试已通过。公开 JSON 同时保留每个终态与包级失败信息，不只记录总数。

首次 Compose 启动命令虽然退出 0、内部服务健康，但当时缺少向宿主发布网页端口。因此原 `docker-first-up` 只证明当时内部启动状态；端口配置修正并重建 Web 容器后，另记录 `docker-port-fix`，之后的三轮 HTTP 与浏览器操作才证明宿主页面可达。不能从初次日志输出的访问地址推导端口已可访问。

相较三轮完整 HTTP 运行，执行方核对最后源码差异仅为三处错误字符串首字母小写。最终镜像已重新构建并实际启动，随后九项保留检查通过；这次新增证据的范围是启动及保留验证，不将它计作第四轮完整 HTTP 流程。

## 保留的本地 Linux 集成失败

容器中的首次 Linux 真实依赖 race **实际退出 1**：顶层 165 PASS、3 SKIP、5 FAIL，必需门禁未通过。它完整保存在 `linux-integration-race`，作为历史非必需来源，不会阻止区分后续宿主集成结果，也不会被删除、改写成 PASS 或合并进成功计数。

失败涉及 Compose 发布口径断言、非回环网络断言、Kafka 保留截断、Redis OOM 保护和重建时真实保留截断子场景。执行方排查发现容器缺 Docker CLI、故障 Redis 的固定回环端口保护不符合该容器运行地址、测试工具默认 Compose 模式，以及网络断言在相关修正前已经编译。原因解释不等于这些用例在该次运行通过；其失败列表仍可逐项检查。

Linux 普通 race、Windows 真实依赖集成与云端 Linux 真实依赖 race 是三个不同范围。**本报告暂未包含本次最终提交的云端 CI 证明**，必须在实际完成后补充精确提交 SHA、运行地址与作业结果，不能套用旧后端审查中的 CI。

## 浏览器与权限结果的边界

浏览器证明来自实际点击、可访问性状态、截图及只读 DOM 尺寸观察，**不是自动化浏览器回归套件**。已检查 390px、768px 下页面没有文档级横向溢出，并恢复视口；这不等于所有窄屏弹窗、键盘焦点循环、屏幕阅读器交互或所有浏览器兼容性已验收。记录中的“没有意外控制台错误”仅限该操作过程。

该次搜索中断仍展示英文诊断文本，恢复时因 gRPC 重连退避，需要稍后再次查询。这些观察保留在原证明和本摘要中，不能用最终构建成功掩盖交互事实。UID 检查仅验证指定容器身份对分离凭据文件的访问规则，不等于生产身份体系、渗透测试或完整安全审计。

项目依然使用模拟来源与模拟执行方，不包含真实硬件协议联调、厂商设备差异或实际巡检效果验证。部署与恢复验收基于本机单实例 Compose，不覆盖跨主机高可用、分区脑裂、自动容灾或生产级容量。当前记录也不替代完整教程的逐阶段复制/联调验收。

## 来源、脱敏及可追溯性

全部原始白名单文件按原始字节 SHA-256 保存到 `.local/archives/fullstack-20260912/<SHA256>/<文件名>`，复制后再次核对。公开记录只保留逐测试四字段、检查名称/布尔值、统计、退出码及来源哈希，不发布 Output、完整请求响应、cookie、访问码或凭证。历史草稿在公开结果更新前另保留私有副本。

这些本机原始来源没有携带足以证明精确被测源码提交的 Git SHA，因而 `exactTestedCommit` 为 null；`archiveRepositoryHead` 只是归档时 HEAD，工作目录当时仍有全栈变更。它不能自动成为每次测试的精确版本。后续最终提交与 CI 应建立独立明确关联。
