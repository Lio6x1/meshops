# 日志与排错课程

用户授权补充排错教学，继续Astra Medium、单人执行。复用现有slog与错误处理，不引入新的日志平台，不改变已验收业务实现。

- [x] 写日志定位说明：真实输出位置、级别与stderr区别、错误码、关联字段和现有缺口。
- [x] 提供完整只读实验脚本：参数缺失、错误凭证、角色不足、非法实体ID，错误后用正确请求复验；保存本次结果，不打印凭证。
- [x] 在当前参考环境验证实验，明确是否用了真实RPC，结束停止本次启动的程序。
- [x] 补任务停滞排查、修复后回归测试步骤，接入课程目录并记录结果。

七项真实CLI/RPC检查和两项回归测试通过。证据：`docs/learning/from-zero/verification/2026-09-10-debugging.md`。本次新增教学与诊断脚本，未改变最终业务源码及日志基础设施。
