# 搜索前置检查与实现计划

用户已同意顺序：重点检查基础 → 实现一个任务搜索服务与一个索引 → 补课程 → 统一复核。单人执行，不增加任务类型或注册登录系统。

## 先完成基础检查

- [x] 检查凭证与租户授权、参数化 SQL、事务与行锁、版本推进、游标和错误处理。
- [x] 复现并修正明文 RPC 可配置到外部地址的问题；本地模式拒绝外部监听与连接，不假装已有生产 TLS。
- [x] 运行对应单元和真实 MySQL 的隔离、事务与终态回归，同步受影响的课程答案。
- [x] 明确可索引数据：任务实例 `payload.note`、取消/失败原因；TaskDefinition.description 是类型目录说明。执行结果为确定性结构，不随意添加自由文本破坏既有契约。

## 搜索与课程

- [x] 锁定兼容的 Canal/ES 组合，写明任务索引、搜索接口、权限、同步、重试、首次导入及失败恢复设计。
- [x] 实现 MySQL → Canal → Kafka → Go 消费者 → ES，搜索失败不阻塞原任务流程。
- [x] 实现关键词、任务状态、目标实体、时间过滤与有界分页；租户由认证身份决定。
- [x] 补齐不完整初始化和索引/位点丢失后的维护重建，保留现有任务事实数据；维护重建及 11 表校验和验证见 2026-09-12 记录。
- [x] 在原 Z00—Z09 后追加 Z10，完整文件、三步构建、十阶段复制及独立升级恢复通过。
- [x] 专项与整体复核，记录真实验证和未覆盖的生产边界；见 2026-09-12-integrated-review.md。

不因“技术较新”随意升级所有依赖。重点验证具体机制；高熵机器凭证的摘要查找不等同于用户密码哈希。
