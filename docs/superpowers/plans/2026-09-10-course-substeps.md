# Z04、Z07课内文件递进

> 按日期保留的设计/实施记录，不作为当前待办或启动指南。当前工程已演进至 Z13；旧路径和阶段状态按当时上下文理解，后续状态见 [记录说明](../README.md)。

用户已授权继续细化，统一Astra Medium、单人执行；在现有独立课程目录中继续，不修改原框架或最终业务语义。

## 设计

- Z04拆为协议、适配、接入规则、真实RPC四个可编译步骤。协议替换是显式维护步骤，移除旧临时Put接口调用，后续恢复网络入口；不伪造业务函数。
- Z07拆为任务协议/纯规则、数据库任务事实与Outbox、分发、执行inbox、运行装配五个代码步骤，第六单元验证完整链路。纯规则步骤只引入已有域规则及对应三项测试，下一步替换为完整测试文件。
- 完整文件引用已验证阶段源码；仅截取测试职责时提供独立完整文件。每步输出精确增删改清单、源码链接、运行命令和能力边界。
- 一次空目录验证按原阶段及新增小步骤推进，每个小步骤go build ./...及有针对性的真实测试；匹配具体测试名，避免零测试假通过。阶段末与原阶段全部文件逐项核对。
- 二进制协议基线一律直接复制并做SHA256比较，不经过Markdown文本编码。

## 工作

- [x] 修复二进制基线在讲义导出中的文本处理，验证文件hash。
- [x] 输出Z04四步、Z07五步的文件清单和参考测试。
- [x] 在同一新目录连续验证九个小步骤，阶段末代码与原阶段一致。
- [x] 细化每步输入、输出、代码与错误分支解释，更新入口与记录。

结果：见课程 `verification/2026-09-10-substeps.md`。九个构建点与22次指定测试执行通过，二进制基线缺陷修复。没有重跑不受影响的最终故障/性能验收，没有启动子agent。

本计划只细化上述大阶段，不声称全部课程已终审。新单元测试说明不能替代已存在的真实MySQL/Kafka/RPC阶段验收。
