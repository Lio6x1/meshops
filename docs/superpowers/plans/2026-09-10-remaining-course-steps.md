# 剩余阶段细化

用户授权继续Z05/Z06/Z08/Z09；Astra Medium、单人推进。延用已批准的课程结构，不改变最终业务语义。

- [x] Z05拆为Kafka客户端、持久投影与分进程装配。
- [x] Z06拆为六类转换、本地队列、补传协议、订阅与网关装配。
- [x] Z08拆为历史写入与采样、查询与保留验证。
- [x] Z09拆为影子校验、验收工具、维护入口与证据。
- [x] 完整顺序构建；指定真实依赖测试必须PASS且不SKIP；每阶段结束文件hash与既有阶段一致。
- [x] 更新逐步解释、环境前置、目录与验证记录，保留测试能力边界。

20次步骤构建、49次指定顶层测试执行通过，无SKIP；首次依赖退出造成的失败已保留并记录恢复过程。见 `docs/learning/from-zero/verification/2026-09-10-remaining-steps.md`。没有重复完整故障/性能实验，没有新增子agent。

沿用已有Redis/Kafka本地依赖。MySQL测试由已有TestMain创建独立测试数据库；不在业务库执行全表历史清理。恢复步骤选择不切换active指针的独立generation测试，完整影子切换继承既有隔离验收记录。
